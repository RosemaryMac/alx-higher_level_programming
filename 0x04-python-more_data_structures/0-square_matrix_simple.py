#!/usr/bin/python3
def square_matrix_simple(matrix=[]):
    matrix = ([
            [2, 3, 4],
            [5, 6, 7],
            ])

    new_matrix = matrix([])**2
    print(f"new_matrix")
    print(f"matrix")
